version: '3.8'
services:
  backend:
    build: ./backend
    container_name: aws_backend
    env_file:
      - ./backend/.env
    restart: always

  frontend:
    build: ./frontend
    container_name: aws_frontend
    ports:
      - '80:80'
    depends_on:
      - backend
    restart: always
